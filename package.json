{
  "name": "loci-browser-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:browser": "node --test --test-isolation=none 'test/browser/**/*.test.mjs'"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "playwright-core": "^1.62.0"
  }
}
